import java.util.*;

public class age {
	public static void main(String[] arg){
		//Switch Statements test one variable and the relative cases associated with that variable//
		Scanner age = new Scanner(System.in);
		System.out.println("Enter an age ");
		int age1 = age.nextInt(); 
			/* The declared variable must be different than the declared scanner variable.
			 * When creating input, you create a scanner object. The object is determined by " Scanner age = new Scanner(System.in)".
			 * You use this object to tell the JVM to input values via the " int age1 = age.nextLine();". Essentially you need to declare two
			 * variables one for the scanner and the other for storage. The variable for storage is what the user inputs after the prompt.
			 * 
			 */
			
			switch (age1){
			case  1: 
				System.out.println("You can crawl");
				break;
			case 2: 
				System.out.println("You can talk");
				break;
			case 3:
				System.out.println("You can get in trouble");
				break;
			default :
				System.out.println("Pick 1 - 3 dummy!");
				break;
		}
	}
}
